<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title | default("StageBox x Matt Robertson – Case Study") }}</title>
    <link rel="stylesheet" href="/public/styles/styles.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/d3/7.8.5/d3.min.js"></script>
</head>

<body>
    <header class="article-header">
        <div class="container">
            <div class="header-content">
                <a href="#" class="back-link">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
                        stroke-linecap="round" stroke-linejoin="round">
                        <line x1="19" y1="12" x2="5" y2="12"></line>
                        <polyline points="12 19 5 12 12 5"></polyline>
                    </svg>
                    All Articles
                </a>
                <span class="tag">{{ tag | default("DSP Research") }}</span>
                <h1>{{ title }}</h1>
                <p class="subtitle">{{ subtitle }}</p>

                <div class="author-meta">
                    <div class="author-avatar"></div>
                    <div class="meta-info">
                        <strong>{{ author | default("Richard Anthony Sanchez") }}</strong>
                        <span>•</span>
                        {{ readTime | default("8 min read") }}
                        <span>•</span>
                        {{ date | default("January 8, 2026") }}
                    </div>
                </div>
            </div>

            <div class="hero-image-container">
                <!-- Skeleton UI -->
                <div class="skeleton-shape" style="top: 5%; left: 5%; width: 90%; height: 90%; background: rgba(255,255,255,0.02); border: 1px solid rgba(255,255,255,0.1);"></div>
                <div class="skeleton-shape" style="top: 10%; left: 10%; width: 20%; height: 70%; background: rgba(0, 255, 170, 0.05); border-left: 2px solid var(--accent-green);"></div>
                <div class="skeleton-shape" style="top: 20%; right: 15%; width: 40%; height: 30%; background: rgba(255,255,255,0.03);"></div>
                <div class="skeleton-shape" style="bottom: 15%; right: 15%; width: 60px; height: 60px; border-radius: 50%; border: 2px solid var(--accent-coral);"></div>
                <div class="skeleton-shape" style="top: 10%; left: 35%; width: 30%; height: 10px; background: rgba(255,255,255,0.1);"></div>
            </div>
        </div>
    </header>

    <main class="article-body">
        <div class="container">
            <div class="body-header">
                <h1>{{ title }}</h1>
                <p class="subtitle">{{ subtitle }}</p>
            </div>
            {{ content | safe }}
        </div>
    </main>

    <script src="/public/scripts/main.js"></script>
</body>

</html>
